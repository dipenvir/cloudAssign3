<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Amazon Cognito Authentication - Assignment #3</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>Hello from COMP 3962</h1>
            <h2>Assignment #3</h2>
        </header>

        <% if (isAuthenticated) { %>
            <div class="welcome-message">
                <h3>Welcome, <%= userInfo.username || userInfo.email %></h3>
                <p>Here are some attributes you can use as a developer:</p>
                <pre><%= JSON.stringify(userInfo, null, 4) %></pre>
                <a href="/logout" class="button">Logout</a>
            </div>
        <% } else { %>
            <div class="login-prompt">
                <p>Please log in to continue</p>
                <a href="/login" class="button">Login</a>
                <a href="/help" class="button">Help</a>
            </div>
        <% } %>
    </div>

    
</body>
</html>
